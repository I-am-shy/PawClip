# ── 发布产物（只进 Release，绝不进仓库）──────────────────────
# Wails 把最终产物放在 build/bin/，同时在 build/ 下生成平台资源。
# appicon.png 是**输入**，必须入库，所以这里只忽略具体产物，不能整个
# 忽略 build/。
build/bin/
build/windows/
# build/dist/ 是本地的打包输出（scripts/accept.sh 之后手工 ditto 出来的 zip）。
# 它和 build/bin/ 是同一类东西：发布产物只进 Release，不进仓库。
# 之所以单独列出来，是因为 *.zip 不在下面的忽略清单里——最初的版本漏了它，
# `git status` 里就多出一个 10MB 的未跟踪文件，很容易被 `git add -A` 带进去。
build/dist/
*.app
*.dmg
*.exe
*.msi
*.clipbak

# build/darwin/ 里默认全是生成物，**但 Info.plist 例外**：
# wails 对它的处理是"项目里有就用项目的，没有就用内置模板"
# （packager.go 的 processPList → buildassets.ReadFileWithProjectData）。
# 而我们把 LSUIElement=true 写在了里面 —— 那是免抢焦点面板的必要条件
# （DESIGN §0.2），一旦跟着 .gitignore 丢掉，新 clone 构建出的 App
# 会"能显示面板但收不到键盘"，且没有任何测试会变红。
# 所以这里用「先忽略目录、再显式放行一个文件」的写法。
build/darwin/*
!build/darwin/Info.plist

# ── 编译中间物 ───────────────────────────────────────────────
# frontend/dist 是 Vite 产物，但其中 gitkeep 必须入库：
# main.go 用 `//go:embed all:frontend/dist` 打前端资源，embed 一个不存在的
# 目录会直接编译失败——留一个占位文件，`go build ./...` 才能在没有跑过
# npm 的机器上通过（wails build 会先跑 npm run build 把真产物生成进来）。
frontend/dist/*
!frontend/dist/gitkeep

# Wails 自动生成的前端 JS 绑定（每次构建都会重新生成）
frontend/wailsjs/

# Wails 用来判断"package.json 变了没、要不要重跑 npm install"的哈希缓存。
# 纯派生态：删掉只会导致下次构建多跑一次 npm install，没有副作用。
frontend/package.json.md5

*.test
*.out
vendor/

# Node
node_modules/
.pnpm-store/

# ── 本地数据（绝不入库）──────────────────────────────────────
*.db
*.db-wal
*.db-shm
pawclip.db*
blobs/
backups/

# 说明：assets/icon/dist/ 是**构建输入**（Wails 的 appicon / 托盘图都从这里取），
# 不是发布产物——它由 build-icons.cjs 从 pawclip-source.png 生成，约 3.3 MB。
# 选择入库是为了保证 `git clone && wails build` 开箱即用、不依赖 Node 工具链。
# 若将来嫌体积大，可改为忽略整个 assets/icon/dist/ 并在构建前跑一次
# `node scripts/build-icons.cjs`（见 DESIGN.md §15.4）。

# ── 编辑器 / 系统 ────────────────────────────────────────────
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# 环境与密钥
.env
.env.local

# 工作区临时目录（演示脚本的隔离数据、临时产物），不入库
.workbuddy/tmp/
